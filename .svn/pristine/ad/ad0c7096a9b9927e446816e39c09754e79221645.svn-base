@extends('layouts.masterpage');

@section('content')
    <script src="/js/ProjectDevelopment/AddProject.js"></script>
    <!--add project form-->
    <form id="AddProjectForm" class="form" class method="POST">
        <div class="form-group">
            <label for="referenceNumber">開發案代號</label>
            <input type="text" name="referenceNumber" value="">
        </div>
        <div class="form-group">
            <label for="ProjectName">開發案名稱</label>
            <input type="text" name="ProjectName" value="">
        </div>
        <div class="form-group">
            <label for="ClientID">顧客名稱</label>
            <select id="ClientID" name="ClientID">
                @foreach($ClientList as $list)
                    <option value="{{$list->id}}">{{$list->reference}}</option>
                @endforeach
            </select>
        </div>
        <div class="form-group">
            <label for="NodeID">業務員</label>
            <select id="NodeID" name="NodeID" onchange="GetSales()">
                <option value="">請選擇單位</option>
                @foreach($NodeList as $list)
                    <option value="{{$list->id}}">{{$list->reference}}</option>
                @endforeach
            </select>
            <select id="SalesID" name="SalesID">
            </select>
        </div>
        <input type="button" class="btn btn-primary" name="" value="儲存" onclick="DoInsert()">
        <a href="/PD/ProjectList" class="btn btn-default">取消</a>
    </form>
@endsection